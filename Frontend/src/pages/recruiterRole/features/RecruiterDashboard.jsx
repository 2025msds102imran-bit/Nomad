/**
 * Recruiter dashboard reuses the shared dashboard (same as Company dashboard).
 * Content lives in commonPages/DashboardHome for a single source of truth.
 */
export { default } from '../../commonPages/DashboardHome';
